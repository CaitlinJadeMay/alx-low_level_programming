#include <stdio.h>

int add(int c, int s)
{
return (c + s);
}
int sub(int c, int s)
{
return (c - s);
}

int mul(int c, int s)
{
return (c * s);
}

int div(int c, int s)
{
return (c / s);
}

int mod(int c, int s)
{
return (c % s);
}
